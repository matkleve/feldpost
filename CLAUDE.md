# CLAUDE.md

Read [`AGENTS.md`](AGENTS.md). It is the single instruction file for this repository, for every tool.

Do not add project rules here. Tool-specific files are pointers only — a second instruction file is a second set of rules that drifts silently from the first.
