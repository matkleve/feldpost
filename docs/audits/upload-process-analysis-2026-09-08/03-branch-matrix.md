# 03 — Branch matrix (Phase 3)

**Commit:** `8e4b1e09` · **Method:** static reading, same discipline as Phase 2. **No test in this repository can execute** (`00-baseline.md` § 4), so the *Test coverage* column reports tests **as written**, never as passing.

Column contract per plan § 4 Phase 3: `Branch | Trigger | Governing spec clause | Implementation path | Matches spec? | Test coverage | Notes`.

Verdict vocabulary: **yes** (implementation matches the governing clause), **no** (it does not — the Notes say which side is wrong), **partial**, **n/a** (no governing clause exists), **unverified** (+ the exact check needed).

Path shorthand: `core/…` = `apps/web/src/app/core/…`, `features/…` = `apps/web/src/app/features/…`. Spec shorthand: `pipeline.md` = `docs/specs/service/media-upload-service/upload-manager-pipeline.md`; `routing.md` = `…/upload-manager-pipeline.location-routing.supplement.md`; `dedup.md` = `…/upload-manager-pipeline.dedup-scope.supplement.md`; `manager.md` = `…/upload-manager.md`; `panel.md` = `docs/specs/component/upload/upload-panel.md`; `tray.md` = `docs/specs/component/upload/upload-resolver-tray.md`.

---

## 1. Intake

| Branch | Trigger | Governing spec clause | Implementation path | Matches spec? | Test coverage | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| I1 Multi-file `submit()` | file input change | `manager.md` § Public API | `apps/web/src/app/features/upload/upload-panel/upload-panel-input-handlers.ts:59-69` → `core/upload/upload-manager.service.ts:267` → `core/upload/manager/upload-manager-submit.util.ts:30-73` | yes | `upload-manager.service.spec.ts` (37 `it`), `upload-panel.intake.spec.ts` (4) | `submit()` is **not awaited** at any of the three call sites (`:52`, `:62`, `:150`) — a `classifyBatch` rejection becomes an unhandled rejection (Phase 2 F11). |
| I2 Folder via File System Access | `submitFolder(dirHandle)` | `pipeline.md` Action 2a/3/4 | `…/upload-manager-submit.util.ts:75-150`; capability flag `core/folder-scan/folder-scan.service.ts:97-99` | yes | `upload-folder-upload.integration.spec.ts` (8), `upload-manager-submit.util.spec.ts` (6) | Batch starts `status:'scanning'`, flips to `'uploading'`/`'complete'` at `:112-116`. |
| I3 Folder via `submitWebkitFolder()` | FSA unavailable | `routing.md` § Webkitdirectory fallback (5-row path matrix) | `…/upload-manager-submit.util.ts:153-205`; scan at `core/folder-scan/folder-scan-from-file-list.helpers.ts` | **unverified** | 3 test titles mention `webkit` | The spec's 5-case path matrix (backslash normalisation, empty `webkitRelativePath`, umlaut folders) was not checked line-by-line. **Check needed:** read `folder-scan-from-file-list.helpers.ts` against the matrix rows. |
| I4 **Drag & drop of files** | drop on the panel drop zone | `panel.md`, `upload-panel.lane-and-row-actions.md` | `…/upload-panel-input-handlers.ts:46-57` | yes | `upload-panel.drag-lanes.spec.ts` (12) | — |
| I5 **Drag & drop of a folder** | dropping a directory | `pipeline.md` Action 2a implies folder intake | `…/upload-panel-input-handlers.ts:50` reads **only** `event.dataTransfer.files` | **no — code gap** | none | `dataTransfer.files` does not expand directories; that requires `dataTransfer.items` + `webkitGetAsEntry()`, which appears nowhere in the subsystem (`grep -rn "webkitGetAsEntry" apps/web/src` → 0). Dropping a folder therefore submits nothing, while the picker path supports it. Severity candidate `medium`. |
| I6 Empty selection | picker cancelled / empty folder | `pipeline.md` Action 2a "no jobs means no side effects" | `…/upload-panel-input-handlers.ts:139,149`; `…/upload-manager-submit.util.ts:118-123,177-180` | yes | none named | Explicit comment at `…/upload-manager-submit.util.ts:119-120` documents that project auto-create must not fire for an empty folder. |
| I7 Unsupported type | any file outside `ALLOWED_MIME_TYPES` | `pipeline.md` Action 1; `docs/security-boundaries.md` § 4.4 | `core/upload/support/upload.service.util.ts:68-85`, catalogue at `core/upload/support/upload-file-types.ts:5-28` | yes (client side) | `upload.service.spec.ts` (45 `it`), incl. "ALLOWED_MIME_TYPES covers image, video, and document types" | Server-side mirroring is Phase 8. `'application/csv'` is in the allow-set (`upload-file-types.ts:27`) but `resolveUploadMimeType` never produces it (`.csv`→`text/csv`, `upload.service.util.ts:61-62`) — a dead entry unless the browser reports it directly. |
| I8 Oversized file | `> 25 MiB` | `pipeline.md` Action 1 | `upload.service.util.ts:69-75`; constant `upload-file-types.ts:2` | yes | in `upload.service.spec.ts` | Message is hardcoded English (§ 7). |
| I9 Mixed media types in one batch | photos + documents together | `pipeline.md` Action 1 | no branch — each job is typed independently at `upload.service.util.ts:218-222` | n/a (no clause found) | none | No batch-level homogeneity rule exists in code or spec. Recorded as an unspecified area, not a defect. |
| I10 Folder named `Project: [name]` | folder label matches `/^\s*project\s*:\s*(.+)$/i` | `pipeline.md` Action 2a | `…/upload-manager-submit.util.ts:207-241` | yes | `upload-manager-submit.util.spec.ts` | Matches active projects case-insensitively, else creates a draft project and renames it (`upload-manager-facade-deps.util.ts:101-108`). Failure is swallowed (`catch { return undefined }` at `:238-240`) — the upload proceeds unassigned with no user signal. |
| I11 Nested folder address hierarchy | `Wien/Hauptstrasse 5/…` | `pipeline.md` Action 3/4; `upload-location-config.md` | `…/upload-manager-submit.util.ts:253-270` (bounded by `maxDirectorySegmentsForHint`, ordered by `folderHierarchyTraversalOrder`, gated by `folderHintRequireHighConfidence`) → `upload-manager-facade-deps.util.ts:84-98` | yes | `upload-folder-upload.integration.spec.ts` | Root-label fallback controlled by `folderHintUseRootFallback` (`…/upload-manager-submit.util.ts:270`). |
| I12 Replace entry point | media detail → replace file | `pipeline.md` § Replace | `core/upload/upload-manager.service.ts:390-392` → `core/upload/manager/upload-manager-actions.util.ts:144-184` | yes (intake only) | **1** test title mentions "replace", and it is about level-map entries, not the pipeline | Creates its own single-file batch labelled `'Replace photo'` (hardcoded English, § 7). |
| I13 Attach entry point | media detail → attach to photoless row | `pipeline.md` § Attach | `…/upload-manager.service.ts:402-404` → `…/upload-manager-actions.util.ts:186-229` | yes (intake only) | **0** tests mention attach | Two `console.log` calls in the production path (`upload-manager-actions.util.ts:191-195,227`). |

---

## 2. Media types

| Branch | Trigger | Governing spec clause | Implementation path | Matches spec? | Test coverage | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| M1 Photo JPEG/PNG/WebP | `PHOTO_MIME_TYPES` | `pipeline.md`; `dedup.md` Tier A | `upload.service.util.ts:218-222`; hash `core/upload/support/content-hash.util.ts:122-136` (`photo_v1`) | yes | `content-hash.util.spec.ts` (14) | — |
| M2 **HEIC/HEIF conversion — success** | `isHeicUploadFile` true | `routing.md` Phase 0 | conversion kicked off in parallel with EXIF at `core/upload/pipelines/new/upload-new-prepare-route.util.ts:236-277`; `convertHeicToJpegUploadFile` at `upload.service.util.ts:133-155` | yes | 2 titles mention HEIC, both about the tray gate | Renames `.heic`/`.heif` → `.jpg` (`:142-148`); if the name has neither suffix it **appends** `.jpg`, producing `foo.png.jpg`. Cosmetic. |
| M3 **HEIC conversion — failure MUST terminate the job and MUST NOT upload the original blob** | `heic2any` throws | `routing.md` Phase 0 (normative MUST) | throw at `upload.service.util.ts:151-154`; caught + `ctx.failJob(...,'converting_format',...)` at `…/upload-new-prepare-route.util.ts:255-263`; upload gate re-checks and fails at `core/upload/pipelines/new/upload-new-run-upload-phase.util.ts:68-75`; belt-and-braces guard `throw new Error('HEIC conversion did not produce a JPEG file')` at `…/upload-new-prepare-route.util.ts:76-79` | **yes** | **none** | The rule holds, but by two independent mechanisms with a shared module-level `Map` (`heicConversionByJobId`, `…/upload-new-prepare-route.util.ts:22`). After a failed conversion the map entry is deleted in `finally` (`:56-58`, `:268-270`), so a later `awaitHeicConversionForUpload` **starts a second full `heic2any` decode** (`:47-60`) that is guaranteed to fail again. Wasteful, not incorrect. The rethrow at `:263` lands in a promise that nothing awaits on the held/tray path → unhandled rejection (Phase 2 F4 class). |
| M4 Video | `VIDEO_MIME_TYPES` | `dedup.md` Tier C — "Yes (identical bytes only)" | `core/upload/support/upload-dedup-eligibility.util.ts:4-6` returns **true** for video; hash `binary_v1` (`content-hash.util.ts:138-144`) | yes — **and the plan is wrong** | 1 title mentions video (the MIME-catalogue test) | Plan § 4 Phase 3 states "video (dedup skipped)". Code and `dedup.md` agree that video **is** deduped. Recorded as a plan error, not a code defect. |
| M5 13 document/office types | DOC, DOCX, ODT, ODG, TXT, XLS, XLSX, ODS, CSV, PPT, PPTX, ODP, PDF | `pipeline.md`; `dedup.md` Tier B | all 13 present in the extension fallback `upload.service.util.ts:37-62` and in `ALLOWED_MIME_TYPES` (`upload-file-types.ts:14-27`) | yes | `upload.service.spec.ts` | `resolveUploadMediaType` classifies **anything not photo/video** as `'document'` (`upload.service.util.ts:218-222`) — safe only because validation runs first. |
| M6 Document preview generation follow-up | document reaches `complete` | Actions 8a2/8a3 (non-blocking) | `core/upload/support/upload-thumbnail-persist.util.ts` via `void persistThumbnail(...)` at `core/upload/pipelines/new/upload-new-post-save.util.ts:291-293`; generation service `core/media-thumbnail/media-preview-generation.service.ts` | **partial** | none | Non-blocking as required, but the `void`-ed promise has no `catch`, so a worker outage is silent for the user (Phase 2 F4). |
| M7 Document with no resolvable location | document, no coords, no address | `pipeline.md` Action 6 | `…/upload-new-prepare-route.util.ts:197,204` (`resolveMediaType`) **and** `core/upload/pipelines/new/upload-new-post-save.util.ts:256-259` (raw MIME prefix) | **no — two divergent rules** | `document` appears in 4 test titles | Phase 2 F6. For a file whose browser `type` is `''` the two classifiers disagree, so the same file becomes `document_unresolved` on one path and `missing_gps` on the other. |

---

## 3. Dedup

| Branch | Trigger | Governing spec clause | Implementation path | Matches spec? | Test coverage | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| D1 Same-user resume → silent skip | `match.registeredByUserId === currentUserId` | `dedup.md` § Behavior matrix | `core/upload/support/upload-dedup-match.util.ts:6-11,33-45` → `core/upload/support/upload-dedup-skip.util.ts` | yes | `upload-dedup-match.util.spec.ts` (3) | `phase='skipped'`, label "Already uploaded". |
| D2 Colleague duplicate → issue + modal | same org, different uploader | `dedup.md` § Behavior matrix | `…/upload-dedup-match.util.ts:47-63` — `phase='missing_data'`, `issueKind:'duplicate_file'`, `duplicateDetected$` | yes | `upload-dedup-match.util.spec.ts` | — |
| D3 `use_existing` | duplicate modal choice | `dedup.md`; `upload-panel.dedup-ux.supplement.md` | `features/upload/upload-panel/upload-panel-dialog-actions.service.ts` (`DuplicateResolutionChoice` at `:43`) | **unverified** | none named | `dedup.md`'s last AC — "`use_existing` links project context when batch has project filter" — is the **only unchecked box** in that file. **Check needed:** trace `use_existing` through `upload-panel-dialog-actions.service.ts` to the project link write. |
| D4 `upload_anyway` | duplicate modal choice | `dedup.md` — "New media row + storage object; org hash row unchanged" | `core/upload/upload-manager.service.ts:423` → `core/upload/manager/upload-manager-actions.util.ts:275-299` sets `forceDuplicateUpload:true`, clears the issue, re-queues | yes | none | Bypass honoured at `core/upload/support/upload-dedup-check.util.ts:30`. Guard accepts `phase==='skipped'` or `missing_data` + duplicate issue (`:280-286`). |
| D5 `reject` | duplicate modal choice | `upload-panel.dedup-ux.supplement.md` | `DuplicateResolutionChoice` union at `…/upload-panel-dialog-actions.service.ts:43` | **unverified** | none | **Check needed:** find the handler for `'reject'` and confirm it dismisses rather than re-queues. |
| D6 "Apply to all in batch" | bulk duplicate resolution | `upload-panel.dedup-ux.supplement.md` | `features/upload/upload-panel/upload-panel-bulk-actions.service.ts` | **unverified** | none | **Check needed:** read the bulk service for a batch-wide duplicate decision. |
| D7 `photo_v1` vs `binary_v1` | media type | `dedup.md` § Hash algorithms | `content-hash.util.ts:116-145`; `photo_v1` = head+size+GPS+date+direction (`:91-100`), `binary_v1` = head+size+`\|algo=binary_v1` (`:103-113`) | yes | `content-hash.util.spec.ts` (14) | Only the first 64 KiB is hashed (`:17`). Two large files sharing a 64 KiB head **and** exact size collide. For `binary_v1` (documents) that is reachable — e.g. two Office files from the same template padded to the same length. Spec does not acknowledge the truncation risk. Severity candidate `medium`. |
| D8 Org scoping | any dedup lookup | `dedup.md` § Dedup scope — key `(organization_id, content_hash)` | client sends only the hash (`core/upload/manager/upload-manager-dedup.util.ts:9-11`); scoping is entirely server-side in `check_dedup_hashes` | yes in TS; SQL verified in Phase 8 | none | Correct design: the client cannot widen the scope. |
| D9 Hash row whose storage object is gone | orphaned `dedup_hashes` row | `dedup.md` § Orphan guard — "joined `media_items.storage_path IS NOT NULL`" | migration `supabase/migrations/20260311300001_fix_dedup_check_orphans.sql` | **unverified at runtime** | none | Static check in Phase 8. **Check needed:** query the hosted `check_dedup_hashes` definition. |
| D10 Dedup runs twice per job | every non-`optional` job | not specified | `core/upload/pipelines/new/upload-new-pre-resolve.util.ts:336` and again `:364`/`:378`/`:389` | **no — code defect** | none | Phase 2 F5. One redundant RPC per file and a duplicate `dedup_check` transition. |

---

## 4. Location routing

| Branch | Trigger | Governing spec clause | Implementation path | Matches spec? | Test coverage | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| L1 `locationRequirementMode: 'required'` | default | `routing.md` § Panel location mode FSM | `…/upload-new-prepare-route.util.ts:298-300`; cold-start default in `features/upload/upload-panel/upload-panel-signals.service.ts` | yes | `upload-panel.placement.spec.ts` (12) | — |
| L2 `locationRequirementMode: 'optional'` | user turns Auto location OFF | `routing.md` — "Strip placement; keep EXIF metadata columns only" | `…/upload-new-prepare-route.util.ts:302-324` clears `coords`/`titleAddress`/`issueKind`; `core/upload/pipelines/new/upload-new-run-upload-phase.util.ts:218-221` forces `savedCoords = undefined`; `core/upload/pipelines/new/upload-new-post-save.util.ts:73-86` completes immediately | yes | `upload-panel.placement.spec.ts` | Three separate enforcement points for one rule. |
| L3 Session override keyed by project filter | toggle while a single project filter is active | `routing.md` § Session override (6-row table) | `features/upload/upload-panel/upload-panel-signals.service.ts` | **unverified** | none named | **Check needed:** read the signals service for `sessionLocationModeOverrides` and assert each of the six rows (restore on switch, not cleared on filter clear, survives panel close, lost on reload). |
| L4 Phase 0 `prepareExifAndFile` | every job | `routing.md` Phase 0 | `…/upload-new-prepare-route.util.ts:223-280` | yes | `upload-new-prepare-route.util.spec.ts` (6) | Sets `parsedExif` only; never `job.coords`. Matches the normative separation. |
| L5 Phase 1 `mergeTitleCandidateOnJob` | high-confidence file/folder text | `routing.md` Phase 1 | `…/upload-new-pre-resolve.util.ts:51-148` | yes | `upload-new-pre-resolve.util.spec.ts` (6) | Low-confidence file parses are demoted to `addressNotes` (`:129-137`) per `pipeline.md` § Filename Address Confidence Contract. |
| L6 Phase 2 text geocode runs even when EXIF exists | high-confidence text present | `routing.md` Phase 2 (explicit "even if EXIF metadata present") | `…/upload-new-pre-resolve.util.ts:238-243` reaches forward geocode before any EXIF fallback at `:274` | yes | `upload-location-resolution.helpers.spec.ts` (13) | — |
| L7 Phase 3 source agreement within `sourceAgreementRadiusMeters` | `titleAddressCoords` **and** `parsedExif.coords` | `routing.md` Phase 3; radius default 150 m | `core/upload/location/upload-location-resolution.service.ts` `finalizePlacementForJob`, called at `…/upload-new-pre-resolve.util.ts:222,255`; conflict registration `core/upload/location/upload-location-source-conflict.service.ts:84-88` | yes (structurally) | 21 titles mention "source" | — |
| L8 `disambiguationKind:'source'` tray, candidates `source-text` / `source-exif` / `source-both` / `source-none` | disagreement beyond the radius | `routing.md` Phase 3 table | `…/upload-location-source-conflict.service.ts`, candidate builder `core/upload/location/upload-location-precedence.helpers.ts` `buildSourceConflictCandidates` | **partial — verified** for the four ids and the `queryKey = source\|{groupingKey}` shape (`…/upload-location-precedence.helpers.ts` `buildSourceConflictQueryKey`, used at `…/upload-location-source-conflict.service.ts:72`) | `upload-location-precedence.helpers.spec.ts` (10) | The per-candidate placement effects of the spec's 4-row table were **not** verified row-by-row. **Check needed:** read `applySourceConflictChoiceToJob` against each row, especially `source-exif`'s "else folder text when `titleAddressCoords` present; else Issues" fallback chain. |
| L9 Idempotency — at most one open group per `(batchId, queryKey)` | repeated registration | `routing.md` Phase 3 "Idempotent" | choice store `…/upload-location-source-conflict.service.ts:43` (`resolvedSourceChoices`), in-flight map `:45` (`sourceConflictInflight`) | yes (mechanism present) | none named | The in-flight map is the singleflight the spec requires for `finalizePlacement`. **But** `registerDisambiguationGroup` has **five caller services / eight call sites** (`01-structure.md` § 3.2), so idempotency rests on the store, not on a single writer. |
| L10 Late-joining jobs / `held_source_conflict` replay | a job reaches Phase 3 after the group resolved | `routing.md` § Replay hook | `getSourceConflictChoice(batchId, groupingKey)` at `…/upload-location-source-conflict.service.ts:65-73`; applied by `applySourceConflictChoiceToJobId` `:91-112` | yes | none named | `applySourceConflictChoiceToJobId` sets `setPhase(jobId,'queued')` at `:105` — a **backward** transition `awaiting_disambiguation → queued` (Phase 4). |
| L11 **Forbidden default-to-text shortcut for late jobs** | after resolve | `routing.md` Phase 3 "**Forbidden**" | `…/upload-location-source-conflict.service.ts:99-112` — placement comes from `applySourceConflictChoiceToJob(job, candidateId, …)`, i.e. the stored candidate, and the non-placement outcomes go to `missing_data`, never to text | **yes** | none | No shortcut found. Marked as **verified-absent** rather than untested. |
| L12 City ambiguity / Branch C tray | multiple city candidates | `…/upload-address-resolution.branch-c-city-tray.md` | `core/upload/location/upload-location-tray-flow.service.ts` (509 LOC), `core/upload-resolver-tray-orchestrator/adapters/upload-location-tray-producer.adapter.ts` (349 LOC) | **unverified** | `upload-location-tray-flow.service.spec.ts` (7), `upload-resolver-tray.helpers.spec.ts` (16) | **Check needed:** read the Branch C spec against `upload-location-tray-flow.service.ts` step by step. Not attempted — this is the single densest area and would need its own pass. |
| L13 Branch A — no text coords, no EXIF | geocode failed and no EXIF | `routing.md` § Branch A | `…/upload-new-pre-resolve.util.ts:279-283` → `routeJobToMissingData` (`…/upload-new-prepare-route.util.ts:191-216`) | yes | none named | Spec says Phase 3 source-conflict does **not** run for Branch A; code reaches Branch A only after both other tests fail, so the ordering holds. |
| L14 Branch B / EXIF-only placement | geocode failed, EXIF present | `routing.md` Phase 4 | `…/upload-new-pre-resolve.util.ts:165-181,268-277` | yes | `upload-location-precedence.helpers.spec.ts` | — |
| L15 Project location is bias-only | project context present | `routing.md` Phase 5; two `@deprecated Removed` markers | `core/upload/location/upload-location-resolution.service.ts:100`, `core/upload/location/upload-location-tray-flow.service.ts:83` | **unverified** | none | Plan § 6 lead 4. Deferred to Phase 6 with the rest of the deprecation sweep. |
| L16 Geocode far-hit filter vs org `contextDistanceMaxMeters` | Photon hit far from the anchor | `routing.md` Phase 5 + § Distance radii | `core/upload/location/upload-location-geocode-group.service.ts` (293 LOC) | **unverified** | 13 titles mention geocode | `routing.md` § Phase 5 says `registerContextDistanceGroup` should be implemented "only after adapter ships"; the adapter spec `…/adapters/upload-project-gps-reference.adapter.md` exists. **Check needed:** grep for `registerContextDistanceGroup` and compare with the adapter contract. |
| L17 Geocode failure / timeout | geocoder 5xx or no hit | `routing.md` § Webkit fallback ("no per-street typo table … or the job lands in Issues") | `…/upload-new-post-save.util.ts:245-266` (`routeUnresolvedAfterFailedGeocode`), `…/upload-new-pre-resolve.util.ts:264-271` | yes | `upload-location-geocode-outcome.util.spec.ts` (6) | Post-save failure lands in `missing_data` **after** the `media_items` row already exists — so an Issues-lane job can have a persisted row. Consistent with L20 but not stated in the spec. |
| L18 `addressNotes[]` retention | low-confidence filename parse | `pipeline.md` Action 11c | written `…/upload-new-pre-resolve.util.ts:131-137`; persisted `core/upload/support/upload-file-persist.util.ts:190` (`address_notes`) | yes | none named | Defaults to `[]` when empty, never `null`. |
| L19 Low-confidence parse → Issues, not resolved | `score < titleConfidenceThreshold` | `pipeline.md` § Filename Address Confidence Contract | `…/upload-new-pre-resolve.util.ts:127-137` | yes | `upload-new-pre-resolve.util.spec.ts` | — |
| L20 Tray Continue gate | tray presentation active | `routing.md` § Tray Continue gate (2 conditions + text-step exception) | `core/upload/address-resolution/upload-tray-resolution-gate.helpers.ts:12-38` | **partial** | `upload-tray-resolution-gate.helpers.spec.ts` (2) | Both normative conditions are implemented exactly (`:16-19`). The spec's **exception** for `answerKind: 'text'` city steps is **not** in the helper — it must be applied by the caller. **Check needed:** confirm `upload-resolver-tray.component.ts` skips the gate for text steps; if it does not, the city step is wrongly blocked. |

---

## 5. Conflicts & issues

| Branch | Trigger | Governing spec clause | Implementation path | Matches spec? | Test coverage | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| C1 Photoless-row conflict → `awaiting_conflict_resolution`, slot released | `conflictService.findConflict` returns a candidate | `pipeline.md` Action 6 | `…/upload-new-prepare-route.util.ts:326-355` — `setPhase('awaiting_conflict_resolution')` `:340`, `queue.markDone(jobId)` `:342`, `emitLocationConflict` `:344`, `drainQueue()` `:353` | yes — the slot **is** released | 28 titles mention conflict | — |
| C2 `attach_replace` / `attach_keep` / `create_new` | user answers the conflict modal | `pipeline.md` Action 6 | `core/upload/manager/upload-manager-actions.util.ts:251-272` | yes for the three outcomes | `upload-manager.service.spec.ts` | `attach_replace`/`attach_keep` switch `mode` to `'attach'` and set `targetMediaId` from `job.conflictCandidate!` (`:266-270`) — a non-null assertion that throws if the candidate was cleared. |
| C3 **"Requeue at front"** | after conflict resolution | `pipeline.md` Action 6 / `manager.md` — "Re-queues the job at the front of the concurrency queue" (also in the facade doc comment `core/upload/upload-manager.service.ts:412`) | `…/upload-manager-actions.util.ts:258-262` sets `phase:'queued'` and calls `drainQueue()`; selection is `core/upload/manager/upload-manager-queue.util.ts:14-20`, a plain **array-order** filter | **no — code does not implement "front"** | `upload-manager-queue.util.spec.ts` (2) | Nothing reorders the jobs array. A resolved job keeps its original index, so it starts before later-submitted jobs but behind every earlier still-queued one. Either the code needs a priority mechanism or three doc sites need correcting. Severity candidate `medium`. |
| C4 `issueKind` inventory | any issue | `pipeline.md`; `upload-panel.feedback-triage.md` | union `core/upload/upload-manager.types.ts:103-111` (8 members) | **partial** | `upload-phase.helpers.spec.ts` (7) | **Only 6 of 8 are ever written**: `address_ambiguous` (`core/upload/location/upload-location-disambiguation-registration.service.ts:97`), `address_deferred` (`core/upload/location/upload-location-candidate-apply.service.ts:170`), `duplicate_file` (`core/upload/support/upload-dedup-match.util.ts:49`), `missing_gps` (`core/upload/location/upload-location-source-conflict.service.ts:112`, `…/upload-new-prepare-route.util.ts:204`, `…/upload-new-post-save.util.ts:259`), `upload_error` (`core/upload/manager/upload-manager-missing-data.service.ts:45,76`), `document_unresolved` (same two ternaries). **`conflict_review`** is never stored but **is** derived from `phase==='awaiting_conflict_resolution'` at `features/upload/upload-phase.helpers.ts:88-90`, so it is reachable. **`duplicate_photo` has no write site anywhere** yet is read in 10 places (`features/upload/upload-panel/upload-panel-item-helpers.ts:82,95`, `upload-panel-item.component.ts:126,201,211,229,257`, `upload-panel-row-handlers.ts:50`, `upload-phase.helpers.ts:105`). Dead union member kept alive by defensive `\|\|` checks — a direct violation of root `AGENTS.md` § Change-Completeness Rule. |
| C5 **`getIssueKind` falls back to string-matching the status label** | `job.issueKind` unset | not specified | `features/upload/upload-phase.helpers.ts:67-75` matches `'choose location or project'`, `'standort oder projekt'`, `'waehle standort oder projekt'`, `'choose location'`, `'missing location'`, `'standort fehlt'`, `'gps fehlt'` | **no — design defect** | `upload-phase.helpers.spec.ts` | Lane assignment and row actions depend on substring matches against a **presentation string** in two languages, with the German variants written without umlauts. Any copy change or a third locale silently reroutes jobs between lanes. Severity candidate `high` (behaviour), effort `M`. |
| C6 No cross-kind action leakage | issues lane row menu | `upload-panel.lane-and-row-actions.md` | `features/upload/upload-panel/upload-panel-item.component.ts:120-260`, `upload-panel-row-handlers.ts` | **unverified** | `upload-panel.creation-dom.spec.ts` (17), `upload-panel.status.spec.ts` (7) | **Check needed:** enumerate the action set per `issueKind` from `upload-panel-item.component.ts` and diff against the spec's table. |
| C7 Resolving an item must not auto-switch lanes | user resolves an issue | `upload-panel.lane-and-row-actions.md` | `features/upload/upload-panel/upload-panel-lane-handlers.ts` | **unverified** | `upload-panel-lane-handlers.spec.ts` (2) | **Check needed:** confirm no `setLane` call fires from a resolution handler. |
| C8 **Action 8f — changing location on already-persisted media must not create a new job** | `placeJob`/`assignJobToProject` on a job with `mediaId` | `pipeline.md` Action 8f | `core/upload/upload-manager.service.ts:329-341` and `:367-378` branch to `missingData.resolvePersisted*` **before** the re-queue helpers; the helpers themselves would only set `phase:'queued'` (`…/upload-manager-actions.util.ts:111-143`), and `selectQueuedJobsForStart` additionally filters out any job with a `mediaId` (`upload-manager-queue.util.ts:17`) | **yes — belt and braces** | none named | Two independent guards. Verified-present. |

---

## 6. Lifecycle

| Branch | Trigger | Governing spec clause | Implementation path | Matches spec? | Test coverage | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Y1 Retry a failed job, id retained | row action Retry | `manager.md` | `core/upload/manager/upload-manager-actions.util.ts:50-62` | yes — the same `jobId` is reused | "retryFile calls uploadManager.retryJob" | Guard is `phase !== 'error'` → also permits retrying a **cancelled** job, since cancel writes `phase:'error'` (Y3). |
| Y2 Dismiss / dismiss-all-completed | row action / lane action | `panel.md` | `…/upload-manager-actions.util.ts:64-76`; `core/upload/support/upload-job-state.service.ts:146-154` | yes | 1 title mentions dismiss | Both revoke the object URL **only when `phase !== 'complete'`** (`:68`, `:149`). For completed jobs the URL is deliberately handed to `MediaDownloadService` (`…/upload-new-post-save.util.ts:287-289`); whether that service ever revokes it is **unverified** — **check needed:** grep `revokeObjectURL` in `core/media-download/`. If it does not, a large batch retains every full-size object URL for the session. |
| Y3 Cancel a job | row action Cancel | `pipeline.md` § Cancel | `…/upload-manager-actions.util.ts:78-96` | **partial** | 3 titles mention cancel | Two problems. (a) Cancel is modelled as **`phase:'error'`** with `statusLabel:'Cancelled'` (`:88-93`) — there is no distinct cancelled phase, so the panel renders it via `getIssueKind` as `upload_error` and offers Retry. **Fixed 2026-09-10 (P6a, UP-08):** `wasCancelled` flag added; Retry no longer offered. (b) `deps.removeStoragePath(job.storagePath)` (`:85`) deletes the storage object **without deleting the `media_items` row and without awaiting** (`core/upload/upload-manager.service.ts:165-167` fires `.remove()` and discards the promise). Cancelling after the row was saved therefore leaves a row whose `storage_path` points at nothing — the mirror image of Phase 2 F1. Severity candidate `high`. **Fixed 2026-09-09 (P2a, UP-04).** |
| Y4 Cancel a batch | batch action | `pipeline.md` § Batch | `…/upload-manager-actions.util.ts:98-110` + `core/upload/upload-manager.service.ts:320-323` | yes | none named | Iterates non-terminal jobs, then sets `status:'cancelled'` and releases the tray orchestrator. |
| Y5 Logout mid-flight | `auth.user()` becomes null while jobs run | `manager.md` § Ground rules | `core/upload/upload-manager.service.ts:240-247` → `core/upload/manager/upload-manager-effects.util.ts:15-20` → `cancelAllActiveUploads` | yes (mechanism present) | **0** tests mention logout | Runs inside an Angular `effect()`; the cancel path writes signals, i.e. a signal write inside an effect — Phase 7 re-entrancy question. |
| Y6 **`beforeunload` / tab close** | `isBusy()` true | `manager.md:280` — ticked AC "`beforeunload` warning shown when `isBusy()` is true" | listener attached/detached at `core/upload/manager/upload-manager-effects.util.ts:22-28`; handler is `private readonly beforeUnloadHandler = (): void => {};` at `core/upload/upload-manager.service.ts:237` | **no — code is wrong, and the AC is falsely ticked** | none | Phase 2 F2. Compounded by `isBusy` being derived from `activeJobs` = *all non-terminal* jobs (`core/upload/support/upload-job-state.service.ts:109-113`), so it stays true while a job sits in `awaiting_disambiguation` — the listener is attached indefinitely, doing nothing. |
| Y7 Navigate away and back | route change with the panel mounted | `upload-panel-system.md` | subscriptions are `takeUntilDestroyed(destroyRef)`-scoped (`features/upload/upload-panel/upload-panel-lifecycle.service.ts:67-68`); job state lives in a root singleton | yes | none named | Comment at `upload-panel-lifecycle.service.ts:64-65` records the NG0953 fix this scoping was for. |
| Y8 Concurrency limit 3, FIFO start order | queue drain | `pipeline.md` § Queue Draining | `core/upload/support/upload-queue.service.ts:10,18-20`; `core/upload/manager/upload-manager-queue.util.ts:14-20` | yes | `upload-manager-queue.util.spec.ts` (2); 1 title mentions concurrency | Jobs in paused phases (`awaiting_*`, `missing_data`) call `queue.markDone` first, so they do not hold a slot. Verified at `…/upload-new-prepare-route.util.ts:342`, `…/upload-new-pre-resolve.util.ts:211,230,249,257`, `:207`. |
| Y9 Batch `scanning → uploading → complete\|cancelled` | batch lifecycle | `pipeline.md` § Batch | `…/upload-manager-submit.util.ts:96,115,121,173`; completion in `core/upload/support/upload-batch.service.ts` | yes | 5 titles mention batch | `'scanning'` is only ever set by `submitFolder` (`:96`); `submit()` and `submitWebkitFolder` start at `'uploading'`. |
| Y10 **Six phase transitions emit no event** | retry, cancel, place, assign-to-project, resolve-conflict, force-duplicate | `.cursor/rules/ui-state-machine.mdc` § FSM contract | all six write `phase:` through `deps.updateJob(...)` instead of `setPhase(...)`: `…/upload-manager-actions.util.ts:54,88,119,134,258,288` | **no — code defect** | none | `jobPhaseChanged$` is the panel's only phase-change signal (`features/upload/upload-panel/upload-panel-lifecycle.service.ts:81-85`), so these six user actions are invisible to any subscriber. They also bypass `setPhase`'s terminal guard (`core/upload/support/upload-job-state.service.ts:159`). Severity candidate `high`. |

---

## 7. Uploaded-lane follow-ups, and the i18n sweep

| Branch | Trigger | Governing spec clause | Implementation path | Matches spec? | Test coverage | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1 `Open in /media` gated on `mediaId` | uploaded-lane row menu | `upload-panel.lane-and-row-actions.md` | `features/upload/upload-panel/upload-panel-item.component.ts:196-198` (`getLaneForJob(job)==='uploaded' && !!job.mediaId`) | yes | `upload-panel.creation-dom.spec.ts` | — |
| U2 `Prioritize` / `Download` / `Open project` gating | uploaded-lane row menu | same | `…/upload-panel-item.component.ts:104-260`; labels `upload-panel-item-helpers.ts:108-140` | **unverified** | `upload-panel.status.spec.ts` (7) | **Check needed:** assert each action's guard against `storagePath` / `projectId` as the spec requires. |
| U3 Assign to project | row action | `pipeline.md` Action 8f | `core/upload/upload-manager.service.ts:367-378` | yes | 3 titles mention project | Same dual guard as C8. |
| U4 Map-pick / address-finder location change | row action | Action 8f; `panel.md` § Media Item Menu Contract | `features/upload/upload-panel/upload-panel-dialog-actions.service.ts:193,238` | yes | `upload-panel.map-pick.spec.ts` (5) | Emits the **UI-side** `ImageUploadedEvent` `{id,lat,lng}` (Phase 2 F7) — the shape collision that breaks `upload-panel.map-pick.spec.ts:42` at compile time today. |

### i18n sweep (plan § 3 Q10 — completes and corrects Phase 2 F10)

`statusLabel` is **stored** in English but **rendered** through i18n: `features/upload/upload-panel/upload-panel-item-helpers.ts:72-106` routes all 20 phases through `t()` (map at `:12-32`, plus explicit `missing_data`/`complete`/`error`/`skipped` branches). The raw value only escapes where the code bypasses that function. Confirmed leak sites:

| # | Site | What leaks |
| --- | --- | --- |
| 1 | `features/upload/upload-panel/upload-panel-item-helpers.ts:76-78` | `job.error`, returned verbatim ahead of every translation branch |
| 2 | `features/upload/upload-panel/upload-panel-item.component.ts:176` | `label: job.statusLabel` on the progress overlay |
| 3 | `features/upload/upload-panel/upload-panel-menu-action-router.service.ts:118-123` | `toastService.show({ message: job.statusLabel })` |

Every producer of `job.error` is hardcoded English: `core/upload/support/upload.service.util.ts:73` (size), `:81` (type), `core/upload/support/upload-error-messages.util.ts:13,22-27,33-60+` (all `title`/`summary`/`hint`), `core/upload/manager/upload-manager-actions.util.ts:90-91` (`'Cancelled'`, `'Upload cancelled by user.'`). Batch labels `'Replace photo'` / `'Attach photo'` (`…/upload-manager-actions.util.ts:150,205`) are also English literals rendered in the panel header.

`npm run i18n:guard` reports none of these — it scans templates for literal text nodes only (`00-baseline.md` § 2, B7), and these are TypeScript string literals. **The guard has a blind spot for service-produced user-facing strings.** Severity candidate `medium`, and a gate improvement worth proposing in `11-proposals.md`.

---

## 8. Dev flags and the "second tray path" (plan § 6 lead 6 — resolved)

`UPLOAD_DEV_FLAGS` (`features/upload/upload-dev-flags.ts:5-12`):

| Flag | Value | Gates | Verdict |
| --- | --- | --- | --- |
| `dockAlwaysVisible` | `false` | passive dock visibility at `features/upload/upload-shell/upload-shell-ui.service.ts:35,43` and `features/upload/upload-resolver-tray/upload-resolver-tray.component.ts:288,296,307` | live flag, correctly defaulted off; its own comment asks for it to be false before release |
| `mockResolverTray` | `false` | `…/upload-resolver-tray.component.ts:192,285,342` | live flag. It seeds the orchestrator from `upload-resolver-tray.mock-orchestrator.ts` in `ngOnInit` (`:342-347`) — so the **mock path ships in the production bundle**, reachable by flipping one constant. Note it does **not** use `upload-resolver-tray.mock.ts`, which has zero importers (`01-structure.md` § 5). |
| `useTrayOrchestrator` | `true`, documented "production default" | `…/upload-resolver-tray.component.ts:98` | **the lead's premise is refuted.** The component computes `useOrchestrator = USE_TRAY_ORCHESTRATOR \|\| UPLOAD_DEV_FLAGS.useTrayOrchestrator \|\| UPLOAD_DEV_FLAGS.mockResolverTray` (`:96-99`), and `USE_TRAY_ORCHESTRATOR` is a hard-coded `export const USE_TRAY_ORCHESTRATOR = true;` (`core/upload-resolver-tray-orchestrator/upload-resolver-tray-orchestrator.types.ts:10`). `useOrchestrator` is therefore **statically `true`**: flipping the dev flag changes nothing. |

**Consequence:** every `if (!this.useOrchestrator)` branch in the tray component is unreachable — **13 guard sites** at `upload-resolver-tray.component.ts:109,113,118,127,144,151,216,241,278,391,422,429`. Two further constant-folded guards live in `core/upload/location/upload-location-disambiguation-registration.service.ts:124` and `core/upload/support/upload-pre-resolve-wave.service.ts:34,63` (`!USE_TRAY_ORCHESTRATOR || …`).

So there is **no second, non-orchestrator tray path** — only the dead shell of one. Carried to Phase 6 as dead code.

---

## 9. Branches left unverified, with the check each needs

Plan § 8 requires every branch to carry a verdict **or** an explicit `unverified` plus the check. Consolidated:

| ID | Branch | Check needed |
| --- | --- | --- |
| I3 | webkitdirectory path matrix | Read `core/folder-scan/folder-scan-from-file-list.helpers.ts` against `routing.md` § Webkitdirectory fallback, all 5 rows |
| D3 | `use_existing` links project context | Trace `use_existing` in `features/upload/upload-panel/upload-panel-dialog-actions.service.ts` to the project link write; this is `dedup.md`'s only unchecked AC |
| D5 | `reject` duplicate choice | Locate the `'reject'` handler and confirm it dismisses |
| D6 | "apply to all in batch" | Read `features/upload/upload-panel/upload-panel-bulk-actions.service.ts` |
| D9 | dedup orphan guard at runtime | Query the hosted `check_dedup_hashes` definition (needs credentials — Phase 8/10) |
| L3 | session override map, 6 rows | Read `features/upload/upload-panel/upload-panel-signals.service.ts` |
| L8 | the four `source-*` candidates' placement effects | Read `applySourceConflictChoiceToJob` in `core/upload/location/upload-location-precedence.helpers.ts` against `routing.md` Phase 3's 4-row table |
| L12 | Branch C city tray | A dedicated pass over `core/upload/location/upload-location-tray-flow.service.ts` (509 LOC) + `core/upload-resolver-tray-orchestrator/adapters/upload-location-tray-producer.adapter.ts` (349 LOC) against `…/upload-address-resolution.branch-c-city-tray.md` |
| L15 | project-location bias-only removal complete | Phase 6 deprecation sweep |
| L16 | far-hit filter vs `contextDistanceMaxMeters` | `grep -rn registerContextDistanceGroup` and compare with `…/adapters/upload-project-gps-reference.adapter.md` |
| L20 | tray gate's `answerKind:'text'` exception | Confirm `upload-resolver-tray.component.ts` bypasses `areAllJobsReadyForTrayResolution` for text steps |
| C6 | no cross-kind action leakage | Enumerate the per-`issueKind` action set in `upload-panel-item.component.ts` and diff against `upload-panel.lane-and-row-actions.md` |
| C7 | resolving must not auto-switch lanes | Confirm no `setLane` fires from a resolution handler |
| U2 | Prioritize / Download / Open project gating | Assert each guard against `storagePath` / `projectId` |
| Y2 | completed-job object URLs | `grep -rn revokeObjectURL apps/web/src/app/core/media-download/` |

Everything above is a **static** check that a resuming agent can complete without a backend, except D9. Branches requiring the running app are listed in `10-findings.md` under the abandoned Phase 10.
