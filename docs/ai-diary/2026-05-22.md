# AI diary — 2026-05-22

## Session focus

Media detail **Location** section: editable active GPS (map pick, paste coords, address search), in-place updates without reloading the workspace pane, resolving spinners, and a brief primary **text flash** on fields that actually changed. Parallel thread: user renaming **image → media** in code (agent paused when asked).

## What we shipped (end state)

- **Map pick from `/media`** — navigate to `/map`, place pin, return via `returnUrl`; `MediaDetailLocationSyncService` + deferred patch (`queueMicrotask`) so the detail view updates without `loadImage()` / `loading` skeleton.
- **GPS vs address** — active `latitude`/`longitude` in Location; EXIF stays readonly in Details; writes go through `resolve_media_location` RPC, not direct column updates when policy applies.
- **In-place refresh** — `refreshMediaLocationFields()` merges location columns only; `prepareLocationPatchAfterGpsChange()` clears stale street/city when coords change so old city names are not kept while skipping DB refresh.
- **Resolving UI** — `addressFieldsResolving` / `coordinatesResolving`; spinners on value cells; flash runs **after** spinners clear (not while spinner branch is shown).
- **Address search apply** — pick suggestion → RPC → patch all lines + coords → DB refresh → flash **changed** fields only (`collectLocationFieldChanges` with trimmed compare).
- **Flash** — `_detail-row-value-flash.scss`: primary **text** only (~2.75s, no row background); class on `.detail-row__field-trigger` / address trigger.

## What we learned (technical)

| Topic | Lesson |
| ----- | ------ |
| Full pane “reload” | `loadImage()` sets `loading=true` and hides the detail body. Location changes must use patch + `refreshMediaLocationFields`, never full load on map return. |
| Blank `/media` page | **Never write `media` (or other) signals inside an `effect` that reads them.** Schedule patch with `queueMicrotask` after the effect completes. |
| Stuck rows (`—`, `0,0°`) | If patch only has lat/lng + `address_label`, merge kept old street/city; `needsAddressResolutionAfterGps` was false when label existed. Fix: always sync from DB after GPS change; clear omitted address keys when coords change. |
| Flash invisible | Template shows spinner **instead of** value button while `addressFieldsResolving`; flash fired during spinner state. Fix: flash in `finally` after spinners off. |
| Flash on center | Row background + `color` on parent lost to child `.detail-row__field-trigger { color: var(--foreground) }`. Fix: animate color on the value element only. |
| City/country no flash | Same value after pick (e.g. still Vienna, Austria) → correct for **delta-only** flash; user confirmed not to flash unchanged fields. |
| `resolvePendingMediaItem` | Often `changed: false` when status already `resolved`; rely on `refreshMediaLocationFields` after RPC. |

## What went wrong (agent mistakes)

- Treated **active** coordinates like EXIF/readonly when `gps_assignment_allowed` was false (frontend blocked map pick; DB still allows `resolve_media_location`).
- Ran **full detail reload** on location sync early in the arc (user: “don’t reload the whole pane”).
- **Signal writes inside `effect`** for location sync — risked blank page / NG0600; fixed with `queueMicrotask` but should have been the first implementation.
- Briefly flashed **all populated** address rows on search apply; user correctly wanted **only changed** fields — reverted `locationFieldsForAddressSearchApply` merge.

## How our communication can improve

### What helped

- **Concrete symptoms:** “GPS updates but address doesn’t”, “no spinning circles”, screenshot of stuck `—` / `0,0°`, “values appear when I click the row”.
- **Hard constraints:** “don’t reload everything”, “be careful — last try broke the whole page”, **STOP** during image→media rename.
- **Product clarifications:** two coordinate types (EXIF vs active), map pick should return to previous route with pane open.
- **Confirming semantics:** user agreed city/country should **not** flash if unchanged — avoids over-animation.

### What to ask earlier (agent)

Before changing location sync / detail loading:

1. Should this path call **`loadMedia` / `loadImage`** or **patch in place** only?
2. Is the detail pane **still mounted** on `/map` (layout-level) or destroyed?
3. After map pick, should **all address lines** update or only what the geocoder returns?

Before flash UX:

1. Flash **every populated field** or **only deltas**?
2. **Text only** or row background?
3. Rough **duration** (e.g. ~1.5s vs ~3s)?

### What to state before each change (agent)

- **Hypothesis** (one sentence).
- **Files** (1–3 max).
- **Will not touch** (e.g. no `loadImage`, no `effect` signal writes, no full-pane `loading`).
- **Verify** (map pick → back, see spinners, see flash on changed values only).

### When something breaks the page

1. **Stop adding layers.**
2. Name the likely cause (e.g. effect + signal write).
3. Give one minimal fix + “hard refresh”.
4. Ask for **console errors** if still broken.

### What helps from the user (optional)

- “Patch only, no reload” when discussing detail updates.
- Screenshot + whether **clicking the row** fixes display (signals stale UI vs missing data).
- “Delta flash only” vs “flash all fields” when polish on highlights.

## Note to future me

- Read this file before editing `media-detail-view.component.ts` location sync, `media-detail-location-sync.service.ts`, or location section templates.
- **Invariant:** external GPS/address saves → `applyLocationPatch` + `refreshMediaLocationFields`; flash after resolvers clear; compare before/after with `collectLocationFieldChanges`.
- **Never** `media.set` inside an `effect` listening to `mediaDetailLocationSync.lastEvent`.
- Specs: media detail location actions, `resolve_media_location`, location-resolver / address-reconciliation stubs in AGENTS.

---

## Session (afternoon): Media grid / `app-media-display` FSM

### Problem arc

- `/media` grid tiles were blank (content layer stuck at `opacity: 0` because `[data-state]` on `display: contents` host did not reach layer CSS).
- User wanted **shrink-then-reveal**: square placeholder → slot animates to photo ratio → then sharp image fades in (PDF previews too).
- Several wrong turns: invented `visibility` / `image--revealed` / `showSharpContent` (not in spec); duplicate `aspect-ratio` transition on viewport + slot; ratio emitted too early (instant jump); illegal FSM hop `ratio-known-contain → content-fade-in` (guard silently rejected).

### What we shipped (end state)

- **FSM strictly opacity-driven** per `docs/specs/component/media/media-display.md` choreography table — no extra visibility hacks.
- **Geometry ownership:** only `media-item__slot` owns `aspect-ratio` + transition + `border-radius` / clip; `app-media-display` + viewport fill the slot at `100%`; layers react to `data-state` on `.media-display__viewport`.
- **Ratio timing:** `--media-aspect-ratio` stays `1` during `loading-surface-visible`; `commitAspectRatioToSlot()` runs on `ratio-known-contain`; `transitionend` on **slot** (media-item forwards to `media-display.onSlotGeometryTransitionEnd()`).
- **Flow:** loading pulse (1:1) → slot shrink → `media-ready` → `content-fade-in` → `content-visible`.
- **Staged content** = optional blurred `warmPreviewUrl` bridge; grid often skips it visually (layer empty).
- **Token fix (what made shrink work):** `var(--transition-geometry)` is **not defined** (Phase 7 removed bridge aliases). Slot now uses `transition: aspect-ratio 300ms var(--motion-ease-out);` per `docs/design/tokens.md` §3.6 + `motion.md`.

### Key files touched

- `apps/web/src/app/shared/media-display/` (html, scss, ts, state transitions)
- `apps/web/src/app/shared/media-item/` (slot `transitionend`, preview fills slot, aspect ratio signal)
- `apps/web/src/app/shared/item-grid/item-grid.component.scss` (slot tokens for grid modes — earlier in thread)

### What we learned

| Topic | Lesson |
| ----- | ------ |
| **Simple first** | If a transition “does nothing”, check DevTools for **unset CSS variables** before refactoring FSM/TS. `--transition-geometry` was referenced in specs but never on `:root`. |
| FSM guards | `goTo()` that violates `MEDIA_DISPLAY_TRANSITIONS` **fails silently** — state stuck, CSS never applies (e.g. content layer stays `opacity: 0`). |
| Spec vs code | Choreography table allows only documented properties (`opacity`, slot `aspect-ratio`). No `visibility` on `<img>`. |
| Ownership | Parent slot cannot read child CSS; `aspectRatioChange` output is required to mirror ratio on `.media-item__slot`. |
| Staged vs content | **Loading** = grey pulse, no image. **Staged** = optional low-res blur. **Content** = final `resolvedUrl`. User-facing path is often loading → shrink → content only. |

### Agent mistake to avoid

- Jumped to complex FSM/CSS refactors while **`--transition-geometry` was empty** — one-line token fix was the actual shrink bug at the end.
- **Rule for future me:** grep `var(--…)` in `_typography-baseline.scss` / `tokens.md` **first**; specs may still name removed bridge tokens (`--transition-geometry`, `--transition-fade-in`).

### Note to future me (media grid)

- Read `docs/specs/component/media/media-display.md` + rendering-matrix supplement before layer/opacity edits.
- Slot transition: `300ms var(--motion-ease-out)` — not `--transition-geometry`.
- Never `ratio-known-contain → content-fade-in`; always `→ media-ready → content-fade-in`.
- Defer `aspectRatioChange.emit` until `triggerRatioTransition` / `commitAspectRatioToSlot`.

---

## Session (evening): `app-media-location-add-search` — “Add new” row stuck on first character

### Symptom (user)

- Combobox shows full typed text (e.g. `DENBI`).
- Dropdown **Internet** zone updates (`Searching…` on each keystroke).
- **New address** row shows only `+ D` (icon + first character), not full query or i18n label.

### What was **not** the bug (wrong agent hypotheses — cost many tokens)

| Wrong theory | Why it was wrong |
| ------------ | ---------------- |
| **Signals not updating** | Console logs showed `onInput` and `query` updating every keystroke (`D` → `De` → `Den` …). |
| **`app-dropdown-shell` portal / detached CD** | Shell uses `<ng-content />` + `position: fixed`; file comment says **no CDK DomPortal**. Parent component still owns the template. If CD were broken, **Internet “Searching…”** would also freeze. |
| **Need `@Input()` on shell** | Shell does not own listbox markup; projecting inputs would not fix stale views. |
| **Only wrong visible binding** (`addRowText` vs `addNewAriaLabel`) | Format/i18n matter for copy, but user still saw one character while signals were correct — view sync, not string format. |
| **Separate `addRowText` signal required** | Extra signal duplicated `query`; did not fix stale DOM when root cause was CD + earlier input clobbering. |

### Real issues (stacked — all had to be fixed)

1. **`[value]="query()"` on the combobox input**  
   One-way binding reset the native input on change detection; `query` often stayed on the first character even when the field looked fuller.

2. **`effect()` that read `query` and set `input.value`** (introduced during debugging)  
   Effect re-ran every keystroke; `setTimeout` wrote **stale** `query` back into the DOM. **Focus-only effect must only depend on `active()`, never `query`.**

3. **Projected dropdown subtree not re-checked after `query.set()`**  
   Same pattern as working `metadata-property-picker`: after `searchTerm.set()`, call **`ChangeDetectorRef.detectChanges()`** in `syncQuery()`. Without this, signals updated but the New address row DOM did not.

4. **Optional layout clip**  
   Narrow flex row could make a long label look like one character; added `min-w-0`, `overflow: visible`, `white-space: normal` on `.media-location-add-search__add-new-label`.

5. **User-side noise**  
   Broken template (`{{ conso}}`) blocked rebuild; user deleting characters made logs look like regression.

### Fix (end state — `media-location-add-search/`)

| Piece | Rule |
| ----- | ---- |
| Input | **No** `[value]="query()"`; `(input)="onInput($event)"` reads `event.target.value`. |
| State | Single `query` signal; `newAddressRowLabel` computed from `query().trim()`. |
| `syncQuery` | `query.set(value)` + search side effects + **`this.cdr.detectChanges()`**. |
| Focus `effect` | `if (!active()) return;` then `focus()` only — **do not read or write `query` / `input.value`**. |
| Template | `@if (query().trim())`; label span uses **`[textContent]="newAddressRowLabel()"`** (forces text node update). |
| Reference | `metadata-property-picker.component.ts` → `applySearchTerm` + `cdr.detectChanges()`. |

### Agent mistakes (process)

- Chased architecture (portal, extra signals, `markForCheck` theory) **after logs proved signals were fine**.
- Re-introduced the bug while “fixing” (effect + `input.value = query()`).
- Did not **grep the repo for a working dropdown + typeahead** (`metadata-property-picker`) until late.
- Did not ask: **“Does Internet zone update while typing?”** — one question splits signal bug vs view bug.

### One-minute diagnostic for future agents

1. Log `query()` in `syncQuery` — if full string in console but UI shows one char → **view/CD**, not signals.
2. Confirm **other dropdown sections** (e.g. `loadingPlaces` / “Searching…”) update on same keystrokes.
3. Inspect input: remove `[value]="query()"`; inspect effects that touch `input.value`.
4. Compare with `metadata-property-picker` (`detectChanges` after term set).
5. Read `dropdown-shell.component.ts` line 7: **no DomPortal** — do not assume overlay isolation.

### Files

- `apps/web/src/app/shared/workspace-pane/media-detail/media-location-add-search/media-location-add-search.component.ts`
- `apps/web/src/app/shared/workspace-pane/media-detail/media-location-add-search/media-location-add-search.component.html`
- `apps/web/src/app/shared/workspace-pane/media-detail/media-location-add-search/media-location-add-search.component.scss`
- Working reference: `metadata/metadata-property-picker.component.ts`

### Note to future me

- **Invariant:** location add-search combobox is **uncontrolled**; `syncQuery` owns signals + **`detectChanges()`**.
- **Never** bind `[value]="query()"` on `#searchInput`.
- **Never** read `query` inside the open/focus `effect`.
- Spec: `docs/specs/ui/media-detail/media-detail-location-section.md` (zone 4 — Add new Address).
