# 2026-09-15

## F-20 behoben: „Keep" platziert den Job jetzt wirklich

Der Auftrag war knapp — „fix that" — gemeint war der gestern dokumentierte, nicht behobene Fehler:
die Option *„Keep: {street}, {city}"* im `containment_check`-Tray schloss das Tray, ließ den Job aber
für immer in `awaiting_disambiguation` stehen.

### Erst reproduzieren, dann reparieren

Statt sofort zu patchen zuerst der Beweislauf: `trace:upload --answer-trays` (der Harness beantwortet
Trays automatisch mit dem ersten Kandidaten, und `patchContainmentCheckOutcome` setzt „Keep" immer an
erste Stelle). Ergebnis vorher, schwarz auf weiß:

```
[ 285] tray  answer containment_check → Keep: Maria Theresien Straße, Wien
outcome: phase=awaiting_disambiguation  lane=Waiting for user  mediaId=—
lanes: Active=1 Issues=1 Uploaded=16 Waiting for user=3
```

Das Tray war beantwortet, die Frage weg, der Job unbewegt — genau die Signatur aus TRAP-021.

### Was „Keep" eigentlich heißt

Es ist keine Tray-Dismissal, sondern eine **Platzierungsentscheidung**: der Geocoder hat nichts
gefunden, und der Nutzer sagt, der Ordnertext stimmt trotzdem. Also muss der Job genau das bekommen,
was D-10 für Area-only gebaut hat — eine textetablierte Adresse **ohne Koordinaten** — und zurück in
die Queue (`phase: 'queued'` + Drain), sonst bewegt ihn nichts mehr.

Dabei fiel auf, dass das Flag falsch heißt: `areaOnlyLocation` bedeutet in Wahrheit nie
„Area-Präzision", sondern „aus Text etabliert, keine Koordinaten" — für eine behaltene Straßenadresse
(Präzision `houseNumber`!) genauso zutreffend wie für einen reinen Bundesland-Ordner. Umbenannt zu
**`textOnlyLocation`**. Hätte ich den alten Namen stehen lassen, stünde jetzt `areaOnlyLocation: true`
an einer Hausnummer — exakt die Art irreführender Oberfläche, für die TRAPS.md existiert.

`disambiguationResolved$` bleibt bewusst ohne Subscriber. Einen zu verdrahten hätte die Lücke
zugedeckt statt geschlossen: der Mechanismus, der Jobs bewegt, ist die Queue, nicht das Event.
TRAP-021 steht deshalb jetzt auf `pattern open` — die Instanz ist weg, die Form (fünf Aufrufstellen,
null Zuhörer) nicht.

### Gemessen, nicht behauptet

Derselbe Lauf danach: S06 `phase=complete`, `lane=Uploaded`, `coords=—`, `mediaId=media-0017`.
Batch: `Uploaded=17 Waiting for user=2` (vorher 16/3). Dazu ein Integrationstest, der den ganzen Weg
geht (Admin-Tray → Geocode ohne Treffer → Containment-Tray → „Keep" → `complete`) und prüft, dass der
Persist-Layer die Textadresse mit `precision: 'houseNumber'` und **ohne** Koordinaten bekommt. Der
Unit-Test, der bisher das kaputte Verhalten festschrieb (`pendingPartialLocation: true`), prüft jetzt
das richtige.

## F-21 gefunden: ein Tray, das man nicht beantworten kann

Beim Nachzählen des „Nachher"-Laufs fiel S18 auf — der Fall des Owners selbst
(`Mödling/Wilhelminenstraße 141/Wilhelminenstr 141, 1160 Wien.jpg`): **55 Antworten auf dieselbe
Frage** in einem Lauf, von 60 Tray-Antworten der ganzen Batch. Am Ende immer noch
`awaiting_disambiguation`.

Nicht geraten, sondern gegen den echten Klassifizierer und die ausgelieferten AT-Assets gemessen:

```
INITIAL  conflicts=[city: [city:Mödling@L2, city:Wien@L0, state:Wien@L0]]
round 1: picked city=Mödling → city=Mödling state=Wien
         conflicts=[city: [city:Mödling@L0, state:Wien@L0]]
round 2: picked city=Mödling → identisch
round 3: picked city=Mödling → identisch …
```

Der Konflikt ist **feldübergreifend**: Mödling liegt in Niederösterreich, nicht in Wien. Der
AT-Gazetteer-Cross-Check merged den `state`-Eintrag in den `city`-Konflikt, aber
`applyAdminLevelSelectionsToSearchObject` schreibt nur das **gewählte** Feld — die andere Seite des
Widerspruchs bleibt stehen, `detectAreaConflicts` hebt ihn sofort wieder, das identische Tray öffnet
erneut. Ein Fixpunkt, keine Lösung. Die andere angebotene Option (`state: Wien`) schleift genauso,
weil sie `city=Mödling` unberührt lässt. Das Tray listet also Optionen, die es strukturell nicht
einlösen kann — darunter den Wert, den der Ordner selbst behauptet.

Das ist dieselbe Mechanik, die D-11s Korroboration gestern zum Kaskadieren gezwungen hat. Damals als
„bestehendes Produktverhalten, durch G2 belegt" eingeordnet — falsch: es ist dieser Bug, von der
anderen Seite gesehen.

Nicht behoben, weil es eine Produktentscheidung braucht (D-12): das abhängige Feld aus der Antwort
neu ableiten (`city→state`, die Regel existiert bereits und wird nach einer Tray-Antwort nur nie
erneut ausgeführt), oder das ganze Gebietspaket auf einmal fragen. Dazu in jedem Fall eine Schleifen-
Sperre — heute prüft nichts, ob eine Konfliktsignatur schon einmal gestellt wurde.

## D-12 gebaut: eine Antwort, die wirklich antwortet (F-21) — und F-22 gleich mit

Owner-Antwort auf D-12: „lets do C with the loop guard and fix F-22 too". Also Spec zuerst
([cross-field answers supplement](../specs/service/media-upload-service/contradiction-resolution-model.cross-field-answers.supplement.md)),
dann rote Tests, dann Code.

### Regel 1 — die Antwort gewinnt über das, was ihr widerspricht

`applyAdminLevelSelectionsToSearchObject` schrieb nur das gewählte Feld. Neu: sie entfernt zusätzlich
genau die Evidenz, die `detectAreaConflicts` selbst als Widerspruch werten würde (dieselben drei
Cross-Checks, dieselben Prädikate — nichts neu erfunden), lässt dann den bestehenden
Ableitungs-Pass laufen, und `city→state` füllt das abhängige Feld aus der Antwort:
`Mödling` → `Niederösterreich`, als normaler `origin: 'derived'`-Eintrag. Die Lücke war nie eine
fehlende Regel, sondern dass der Ableitungs-Pass nach einer Tray-Antwort nie erneut lief
(`deriveStateFromCity` steigt aus, wenn `state` schon Evidenz hat — richtig für Pfad-Evidenz, falsch
für Evidenz, die die Antwort gerade überstimmt hat).

### Regel 2 — der Loop-Guard, und warum die naheliegende Version falsch gewesen wäre

Erst als signaturbasierter Guard spezifiziert („dieselbe `areaConflictQueryKey` nie zweimal"). Beim
Testen fiel auf: **das hätte Fehlalarm geschlagen.** `detectAreaConflicts` Pass 3 *synthetisiert* für
jede Stadt, in die ein widersprechender Postcode expandiert, einen `city`-Eintrag — die legitime
Anschlussfrage nach einer `state`-Antwort („Mödling, oder das 1160, das Wien sagt?") trägt deshalb
dieselbe Signatur `city|modling,wien` wie die gerade beantwortete Frage, ist aber eine andere,
beantwortbare Frage. Der Guard prüft jetzt **Fortschritt**: hat die Antwort eines der vier
Gebietsfelder bewegt? Wenn nein und es steht noch ein Konflikt → Issues statt nochmal fragen. Spec
entsprechend korrigiert; gut, dass der Test vor dem Glauben kam.

### Regel 3 — F-22

Beide Antwortpfade (`layer_package`, `admin_level_conflict`) endeten mit
`setPhase('resolving_location')` + `void applyPreResolveFromOrchestrator(...)`. Der Drain sieht aber
nur `phase === 'queued'`. Jetzt wird das Ergebnis *abgewartet* und ausgewertet: `'continue'` →
zurück in die Queue + Drain, `'held'`/`'partial'` → jemand anderes besitzt den Job jetzt.

### Rot vor grün, nachgewiesen

Die Implementierung entstand teils vor den Tests für Regel 2/3 — also hinterher mit `git stash` der
drei Quelldateien gegengeprüft: **12 der neuen/geänderten Tests fallen ohne den Fix um**, inklusive
des Integrationstests `(d5)`, der ohne den Fix schlicht in den Timeout läuft (genau S07s Symptom).
Zwei bestehende Tests schrieben das kaputte Verhalten fest („keeps conflict when resolved city still
mismatches state") — umgeschrieben, wie schon beim F-20-Test.

### Gemessen

Derselbe `--answer-trays`-Lauf, drei Stände:

| | vor F-20 | nach F-20 | nach F-21+F-22 |
| --- | --- | --- | --- |
| Uploaded | 16 | 17 | **19** |
| Waiting for user | 3 | 2 | **1** |
| Active (hängt) | 1 | 1 | **0** |
| Tray-Antworten | — | 60 | **7** |

S18 wird **einmal** beantwortet statt 55-mal und landet auf `complete` (`media-0019`); S07 lädt mit
seinen Koordinaten hoch (`media-0017`). Übrig bleibt S05 — eine echte offene Frage (Hausnummer, der
Stub liefert keine) — und S11 in Issues, wo es hingehört: der Pfad enthält keine Adresse.

## Phase 3.1: der Job-Store ist jetzt `O(1)` (F-07)

„Mach die nächsten Steps" — also nicht raten, sondern den Plan lesen: STUDY-006 Phase 3.1 steht als
nächstes dran und blockiert Phase 4. Klasse **Sensitive**, weil es der State-Store eines
zustandsbehafteten Service ist. Also Spec zuerst.

### Erst die Garantien aufschreiben, dann umbauen

Das
[Job-Store-Supplement](../specs/service/media-upload-service/upload-manager.job-store.supplement.md)
hält fest, was der Store *verspricht*, bevor irgendein Code sich ändert — G1 Identität, G2
Einfügereihenfolge, G3 unveränderliche Snapshots, G4 ein Write auf eine unbekannte Id ist ein No-op
**und benachrichtigt niemanden**, G5 eine Benachrichtigung pro echter Änderung, G6 terminale
Entfernung. Der Reihe nach, weil sonst die Tests am Ende nur beschreiben, was der neue Code zufällig
tut, statt zu prüfen, was er schuldet.

G2 war dabei der Punkt, an dem es hätte schiefgehen können: `jobs()` wird an mehreren Stellen als
*Batch-Reihenfolge* gelesen. Ein `Map` erhält die Einfügereihenfolge — deshalb ist es ein `Map` und
kein Objekt-Record, und deshalb steht das als Garantie da und nicht als Implementierungsdetail.

### Der Umbau

Vorher `prev.map()` pro Write und `prev.find()` pro Read — `O(n)` und `O(n)`, bei ~15 Writes pro Job
also `O(n²)` pro Batch. Jetzt: ein `Map` nach Job-Id, ein `revision`-Signal, und `jobs` als
`computed` darüber. Die `O(n)`-Arraybildung passiert damit einmal pro *gelesener* Änderung statt
einmal pro Write. `revision` zählt nur bei echter Änderung hoch — genau das macht G4 und G5
überhaupt prüfbar.

### Rot vor grün

Nur `upload-job-state.service.ts` gestasht, Tests laufen lassen:
`× G4: a write for an unknown id changes nothing and notifies nobody` (1 failed, 8 passed). Die alte
Implementierung ließ `_jobs.update(...)` auch dann laufen, wenn `map` nichts traf — neues Array,
Signal feuert, Consumer rechnen umsonst. Kein dramatischer Bug, aber eine Zusage, die vorher nicht
galt.

### Gemessen, gleiche Maschine, gleicher Tier

| Jobs | `updateJob` | `findJob` | ganzer Batch (15 Writes/Job) |
| --- | --- | --- | --- |
| 100 | 0.0029 → **0.0007 ms** | 0.0023 → **0.0001 ms** | 4 ms → **1 ms** |
| 1 000 | 0.0148 → **0.0007 ms** | 0.0154 → **0.0001 ms** | 222 ms → **11 ms** |
| 5 000 | 0.0972 → **0.0014 ms** | 0.0666 → **0.0001 ms** | 7.3 s → **106 ms** |
| 20 000 | 0.9448 → **0.0010 ms** | 0.3569 → **0.0002 ms** | 4.7 min → **315 ms** |

Flach über 200× Batch-Größe — das war das Abnahmekriterium des Plans („`updateJob` flat across
100 → 20 000"). Der Harness meldet vorher wie nachher identische Lanes
(`Issues=1 Uploaded=16 Waiting for user=4` kuratiert, `Skipped=1 Uploaded=20` generiert), also eine
reine Kostenänderung. Upload-Suite: 402 Tests in 59 Dateien.

Nicht gebündelt mit 3.2 (Fuse-Index-Cache), weil der Plan das ausdrücklich verbietet — zwei
unabhängig testbare Kostenänderungen in einem Commit heißt, man kann hinterher keiner von beiden
eine Zahl zuordnen. 3.2 ist der nächste Schritt; die ~9 ms/Datei Klassifizierung sind das, was ein
Archiv-Import als Erstes spürt.

## Phase 3.2: der Fuse-Index wird einmal gebaut, nicht einmal pro Token (F-06)

Der Plan sagt ausdrücklich: 3.1 und 3.2 **nicht bündeln**. Das ist kein Formalismus — zwei
Kostenänderungen in einem Commit heißt, dass man hinterher keiner von beiden eine Zahl zuordnen kann.
Also separat, und wieder Spec zuerst
([Gazetteer-Lookup-Supplement](../specs/service/media-upload-service/upload-search-object.gazetteer-lookup.supplement.md)).

### Was da stand

`classifyWithFuse` baute `new Fuse(items, …)` — den kompletten Index über 2 114 Gemeinden — bei
**jedem Token, das die Exact-Stufe verfehlt**. Der Exact-Index war längst gecacht (seit F-02), der
fuzzy daneben nicht. Ein Cache neben einem Cache, in derselben Funktion, zwanzig Zeilen auseinander.

### Der Test, der rot sein musste

Die interessante Frage bei einem Cache ist nicht „ist er schneller", sondern „antwortet er gleich".
Also fünf Garantien statt einer: G1 exact schlägt fuzzy (das ist F-02, eine Korrektheitsregel — `Wien`
matcht `Schottwien` mit 0.992), G2 ein Index pro Datensatz, G3 Transparenz, G4 Identitäts-Keying
ohne Leck, G5 leerer Datensatz baut nichts.

Erster Versuch: ein `vi.mock` auf `fuse.js`, das den echten Konstruktor erbt und nur mitzählt. Rot war
danach genau einer — G2, 5 Index-Builds für 5 Tokens statt 1. Sah gut aus.

**War es aber nicht.** Im vollen `verify`-Lauf fiel dieselbe Datei mit *zwei* Fehlern um, und zwar mit
`expected +0 to be 1`: null gezählte Konstruktionen. Der Angular-Test-Builder bündelt `fuse.js`, also
greift `vi.mock` dort gar nicht — in Isolation lief der Test aus einem anderen Grund grün als im
Bundle. Ein Test, der die Zahl, um die es geht, nur durch ein Mock sehen kann, das hier unzuverlässig
ist. Und schlimmer: ich hätte damit zwei Fehler zur Pollution-Schuld des `test`-Gates addiert, die
ich in derselben Sitzung noch als Problem beschrieben habe.

Also umgestellt auf das, was ohne Mock beobachtbar ist: **Identität**. „Einmal pro Datensatz gebaut"
heißt genau „derselbe Datensatz liefert dasselbe Objekt" — `fuseIndexFor` ist dafür exportiert, und
die Behauptung hält unter jedem Bundler. Gegenprobe wie immer: Cache-Lookup entfernt → G2 rot,
wieder rein → grün. Die anderen vier liefen schon vorher grün, und das ist das Ergebnis, nicht ein
Versäumnis: ein transparenter Cache *darf* an den bestehenden Garantien nichts ändern, also sind sie
hinterher Regressionswächter.

Zwei Test-Fixtures musste ich davor schon wegwerfen, die mit Bindestrich und Ziffer gebaut waren
(`Krems-g4b`) — die laufen in die Token-Regexe und werden gar nicht erst als Ort geprüft. Gemerkt,
weil ein Test rot war, den ich nicht rot haben wollte. Ersetzt durch schlicht verschiedene
Ortsnamen pro Datensatz, was G4 sogar direkter prüft.

### Gemessen — und die Vorhersage hielt

Die alte Messung hatte Build (1.06 ms) und Search (2.75 ms) getrennt und daraus „Cachen bringt ~30 %"
abgeleitet. Jetzt gegengeprüft, 2 000 Pfade, beide Namensmodi:

| | camera | neutral |
| --- | --- | --- |
| vorher | 6.967 ms/Datei | 6.860 ms/Datei |
| nachher | **4.886 ms/Datei** | **4.912 ms/Datei** |

**−29 %.** Gruppen (1 483) und Trays (634) auf beiden Seiten identisch — das ist der Check, dass der
Cache keine Antwort verändert hat, nicht nur die Laufzeit.

### Was das *nicht* behebt

F-06 hat zwei Hälften, und das hier ist die kleinere. Die Kosten sind gesunken; **wann** sie anfallen,
ist unverändert: `submitUploadManagerWebkitFolder` wartet weiterhin die komplette
`runClassifyBatchGuarded` ab, bevor `drainQueue()` läuft. Bei 4.9 ms/Datei sind das für 100 000
Dateien immer noch ~8 Minuten eingefrorener Main-Thread, bevor irgendetwas hochlädt. Das ist Phase
3.3, und das ist die, die ein Archiv-Import zuerst spürt. F-06 steht deshalb als *teilweise* behoben
im Register, nicht als erledigt — eine halbe Behebung als ganze zu buchen wäre die Art Buchhaltung,
die später jemanden auf eine grüne Zeile vertrauen lässt, die nicht hält.
