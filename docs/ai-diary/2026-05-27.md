# 2026-05-27 — Address resolution + resolver tray (master plan)

## Shipped

- Parent spec `docs/specs/service/media-upload-service/address-resolution-model.md`
- Branch C: `classifyBatch` → `needsGeocode` (not `needsTray`); Photon `street`+`AT`; `pickDiscriminatingField` ranking; tray 1A numbered / 1A text only on 0 hits; 5c house step
- Removed `registerBatchProjectTrayIfNeeded` address fallback (no-op)
- `UploadPreResolveWaveService`: `notifyScanIdle` after pre-resolve wave, not after `classifyBatch`
- Early dedup tag (`duplicateOfMediaId`) before geocode; upload continues
- `exifAssistRadiusMeters` default **80**; config keys for context check / default country / token fuzzy / max dialogue units
- Tray: `dialogueUnitId`, max **5** units per bundle, carousel by units
- Token normalizer MVP module (`TokenNormalizerService`)

## Deferred

- Step 4 EXIF reverse superset (`lang=en`) — spec’d; wire `GeocodingService` reverse + superset compare in pre-resolve
- Full token lookup table beyond AT states
- Tier placement step 6 explicit admin-centroid helper (partial via existing `metadata_only` / partial paths)

## LIVE CHECK (PO)

Folder “Wolzeile 6”: numbered city (or discriminating field) after Photon; `feldpost:debug:upload-address=1`

## Follow-up — source conflict tray copy

- Question: distance in copy (`{distance}` from haversine); options prefixed Folder / Photo location + both/none; no misleading 100% scores.
- Footer: **Next** when more items/bundles; **Done** on last (removed “Next questions” when nothing queued).

## Layer packages (Phase 1–2)

- **Phase 1:** `upload-search-object.layer-map.ts` + specs; `classifyBatch` → `needsLayerResolution`; `layer_package` tray before Photon; `applyLayerPackageChoice` → flat SO + `integrateResolvedLayerGroups`.
- **Phase 2:** Photon hits filtered by org `contextDistanceMaxMeters`; Branch C CITY-01 forces city_step when EXIF far from auto; source conflict batches by `groupingKey`.
- **Phase 3:** Adapter spec only — [upload-project-gps-reference.adapter.md](../specs/service/media-upload-service/adapters/upload-project-gps-reference.adapter.md); no `registerContextDistanceGroup` yet.
- **Invariant:** Package conflict ≠ source conflict; geocode never runs while `detectPackageConflicts` is non-empty.

## SO door / AT unit parsing + key semantics

- **SO:** `door` field; `Top`/`Tür` → `door` (removed `top` from `STIEGE_RE`); AT slash `25/14`, `15/4/5` via `upload-search-object.unit-parsing.at.ts`.
- **Path fix:** `collapseAtSlashPathSegments` — folder paths split on `/` so `Neustiftgasse 25/14` must merge `25` + `14` before segment parse.
- **Keys:** `grouping_key` still street+house only; `p_door: so.door`; `address_dedupe_key` unchanged (DB).
- **Photon gate:** `unitGeocodeSplitMinMeters` (25); `shouldSplitGroupByPhotonUnitCoords` — trace on ambiguous multi-hit; no unit suffix in geocode request.
- **Specs:** hub in `upload-search-object.md`, `upload-search-object.unit-parsing.at.md`, EX-09/10, config + pipeline keys table.
- **LIVE:** `AT/Wien/Neustiftgasse 25/14/IMG_*.jpg` → door on location row; single Photon hit → one geocode per building.

## Upload resolver tray fixes (Thaliastraße)

- **P0:** `resolvedSourceQueryKeys` + `sourceConflictInflight` — no duplicate source tray; `applyCandidateToGroup` sets `coords` from candidate when `titleAddressCoords` missing; `clearDisambiguationJobFields` clears `statusLabel` to `''`.
- **P1:** Folder row = path; option 1 = reverse-geocode label; footer **Save** on last dialogue unit; carousel hidden when &lt;2 units.
- **P2:** Map upload button = spinner + horizontal bar (not conic ring); tray `(previewLocationCleared)` wired on shell.
- **LIVE:** Re-test folder `Thaliastraße 14` with far EXIF — one source question, pick folder address → jobs queue (not stuck “Choose address”).

## Tray regression (post-plan)

- **IMG in option 1:** `labelFromFolderDisplayPath` wins over SO when `houseNumber` looks like camera token (`IMG 14`).
- **Removed** `upload.resolver.waitingPrepare` UI; Save still gated silently (HEIC + source needs text or EXIF pin).
- **Duplicate tray:** skip final `notifyScanIdle` when early tray fired; orchestrator dedupes by `disambiguationGroupId`.
- **3 uploads:** `UploadQueueService` `MAX_CONCURRENT = 3` — parallel cap, not batch size; rest should dequeue as slots free.

## Tray regression round 2 (Save / 3-of-8 / map dot)

**Symptoms (Thaliastraße folder):** tray showed 1 conflict job but Save with photo location queued/uploaded whole folder; duplicate uploads (`dedup_hashes` 409); same job id restarted in `drainQueue` while still `queued`.

**Fixes (behavior — keep when touching split modules):**

- **Re-upload loop:** `selectQueuedJobsForStart` skips `queued` jobs with `mediaId` and jobs already `isRunning`; `runPipeline` bails when `mediaId` present; no second `drainQueue` from `selectAddressCandidate` after Save (`kickQueueAfterLocationGate` / unblock path only).
- **Save scope:** removed `jobIdsForSourceConflictApply`; source Save applies only `group.jobIds` (tray-eligible). Siblings unblocked via `unblockSiblingsAfterSourceConflictSave` → `finalizePlacement` (folder text / own EXIF), not batch photo GPS.
- **Replay:** stored source choice replays only when `isJobEligibleForSourceConflictGroup(job)` (both `titleAddressCoords` and EXIF on that job).
- **EXIF apply helper:** `applySourceConflictChoiceToJob` — photo choice with no `parsedExif` uses **folder text coords** if present, else `skipped_no_exif`; **no** tray-candidate coord injection for jobs without GPS metadata.
- **Preview dot under clusters:** `createSearchLocationMarker` `zIndexOffset: 2500` (above user pin 2000).
- **8 media chip:** source group `jobIds` = `collectSourceConflictJobIds` (both pins only); not whole folder batch.
- **Parsed from folder:** omit `parsedAddress` when it duplicates folder row leaf (`Thaliastraße 14` case).

**LIVE CHECK (still required after any upload-queue / tray Save change):** folder upload → tray shows only conflict job(s) → Save photo location → only tray file(s) get photo GPS; one DB row per file; console: no duplicate `runPipeline` for same id while first run active.

## Upload core services split (mechanical refactor)

**Goal:** shrink `upload-location-resolution.service.ts` (~1625 lines) and `upload-manager.service.ts` (~709 lines) for maintainability; **no product-rule changes** in the split PR.

**Lint (`apps/web/eslint.config.mjs`):**

- Removed `max-lines: off` on `upload-manager.service.ts`.
- **Error** on `src/app/core/upload/**/*.service.ts`: ≤ **300** lines (code only: `skipBlankLines` + `skipComments`), ≤ **80** lines per function.
- Split agent brief: `docs/agent-workflows/upload-core-services-split-prompt.md`.

**Resulting layout (facades stay public):**

| Facade | ~lines | Delegates to |
|--------|--------|----------------|
| `UploadLocationResolutionService` | ~176 | store, source-conflict, placement, geocode-group, tray-flow, disambiguation-registration, pre-resolve-orchestrator, candidate-apply |
| `UploadManagerService` | ~435 raw | pipeline-host, missing-data, existing `*-actions.util` / `submit.util` / `facade-deps.util` |

**Extra slices (beyond original prompt — OK):** `upload-location-candidate-apply.service.ts`, `upload-location-pre-resolve-orchestrator.service.ts`, `upload-location-disambiguation-registration.service.ts` (+ helpers), `upload-location-geocode-outcome.util.ts`, `upload-location-layer-package-choice.util.ts`, `upload-manager-select-address.util.ts`.

**Post-split audit (this session):**

- ESLint upload `*.service.ts`: **0 errors**; `ng build` OK.
- Upload bug fixes above **present** in split modules (`applySourceCandidateToGroup` → `group.jobIds`; pipeline-host `isJobRunning` + `mediaId` guard).
- Features still import facades only (not internal location sub-services).
- **Vitest:** `upload-location-resolution.service.spec.ts` passes alone (8/8); running together with `upload-manager.service.spec.ts` can flake (`LocalGeoDataAdapter` fetch `/assets/geo/...` — cross-file root injector pollution). Fix later: mock geo in manager spec or isolate file parallelism.

**Why the bugs were hard (for agents):** ~6–7/10 from file length; rest = distributed state (`phase` / `mediaId` / `running` / tray gate) and implicit scope (tray vs folder vs replay).
