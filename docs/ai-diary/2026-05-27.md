# 2026-05-27 — Upload resolver tray orchestrator

## Shipped

- `UploadResolverTrayOrchestratorService`: presentation bundles, 5s **max** collect window, `notifyScanIdle` early close, per-item resolve/skip.
- Tray rewired to orchestrator: bundle-scoped carousel, blocked dependents (1B), dynamic footer (`Next questions`).
- `UploadLocationTrayProducerAdapter`: groups → `enqueueItem`; 1A city pick → `confirmTrayCity` → enqueue 1B; `scanIdle` after `classifyBatch`.
- Spec: `docs/specs/service/media-upload-service/upload-resolver-tray-orchestrator.md` (conversational principle §0).
- Dev mock: `upload-resolver-tray.mock-orchestrator.ts` (city options, not empty text 1A).

## LIVE CHECK (product owner)

After folder upload: bundle should appear soon after scan ends (~1–2s), not forced 5s wait. Second carousel index only within bundle. 1B gray until 1A Continue.

## Mistake to avoid

Do not bind tray carousel to `disambiguationGroups().length` — use orchestrator `activeItems()` only.

## EXIF vs Branch C (IMG_1121)

Filename token `IMG` from `IMG_1121.jpg` was classified as Branch C street → city tray before EXIF placement. Fix: `isExifAuthoritativeOverWeakFilenameStreet` — EXIF wins when street is filename-only, no folder path, all jobs have GPS. Mock tray flag remains `false`.
