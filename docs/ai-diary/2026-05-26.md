# 2026-05-26

## Remote Photon on Hetzner (team default)

- Austria Photon moved off developer laptops to Hetzner CPX22 (`feldpost-services`, Falkenstein). Docker at `/opt/feldpost`; Caddy TLS via sslip.io (`https://178-105-242-74.sslip.io` → `178.105.242.74`).
- `supabase/config.toml` `GEOCODER_FORWARD_URL` points at that HTTPS URL; local `docker-compose.photon.yml` is optional fallback only.
- Docs: `docs/playbooks/remote-photon.md`, updated `supabase/AGENTS.md` and `geocoding-service.md`.
- If VPS IP changes: update Caddy hostname, `config.toml`, and `supabase secrets set GEOCODER_FORWARD_URL` on hosted.

## Upload location precedence (Prompt A)

- `parsedExif.coords` vs `job.coords` split: EXIF columns from metadata; placement from `applyChosenPlacementSource` only; `prepareExifAndFile` no longer sets `job.coords`.
- Phased pre-resolve: text geocode even with EXIF; `sourceAgreementRadiusMeters` (150 m) auto-agree vs `disambiguationKind: source` tray; Branch A `missing_data` when no text coords and no EXIF metadata after geocode fail.
- Phase 5 context-distance tray deferred (Prompt B); types/spec SPEC GAP documented.
- **LIVE CHECK:** Fuchsthalergasse folder — ~80 m auto text + EXIF row; ~2 km → source tray; second `/media` visit.
