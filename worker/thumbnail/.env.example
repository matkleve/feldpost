# Supabase (service role — server only, never commit real values)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=

# Storage bucket for media objects (default: media)
MEDIA_BUCKET_NAME=media

# HTTP listen port
PORT=3001
