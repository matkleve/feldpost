# settings-overlay feature

**Settings overlay** routes and sections (account, locale, theme, invites, shortcuts, etc.) composed as overlay flows on top of the app shell.

**Canonical specs:** [`docs/specs/ui/settings-overlay/`](../../../../../../docs/specs/ui/settings-overlay/README.md) (folder index and linked contracts).
