# Unified selection module

Facade over `WorkspaceSelectionService` — one `selectedMediaIds` store for map, `/media`, and selected items panel.

Spec: `docs/specs/service/unified-selection/unified-selection.md`
