# address-field-suggest service module

Facade: ./address-field-suggest.service.ts

## Purpose

Per-field, hierarchically-constrained address suggestions for media detail address rows.
DB-first (org media_items) + Nominatim via GeocodingService.

## Spec

docs/specs/service/address-field-suggest/address-field-suggest.md
