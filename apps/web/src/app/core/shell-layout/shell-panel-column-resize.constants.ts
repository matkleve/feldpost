/** Default panel column width when grid shell opens (px). */
export const SHELL_PANEL_COLUMN_DEFAULT_WIDTH_PX = 400;

/** Minimum canvas width while resizing (px). @see docs/specs/ui/shell/shell-panel-resize.md */
export const SHELL_PANEL_COLUMN_CANVAS_MIN_WIDTH_PX = 320;

/** Minimum panel width for detail medium layout (px). */
export const SHELL_PANEL_COLUMN_MIN_WIDTH_PX = 480;

export const SHELL_PANEL_COLUMN_WIDTH_STORAGE_KEY = 'feldpost.ui.shell.panelColumnWidth';

/** Legacy workspace pane preference — read once for migration. */
export const LEGACY_WORKSPACE_PANE_WIDTH_STORAGE_KEY = 'sitesnap.settings.layout.workspacePaneWidth';
