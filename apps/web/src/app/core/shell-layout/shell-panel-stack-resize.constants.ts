/** Minimum height for top or bottom panel stack when split (px). */
export const SHELL_PANEL_STACK_MIN_PX = 128;

/** Default top-stack share when no preference is stored. */
export const SHELL_PANEL_STACK_DEFAULT_RATIO = 0.5;

export const SHELL_PANEL_STACK_TOP_EXTENT_STORAGE_KEY =
  'feldpost.ui.shell.panelStackTopExtent';

/** Divider bar thickness between stacks (px). */
export const SHELL_PANEL_STACK_DIVIDER_PX = 2;

/** Matches `var(--spacing-3)` at default root (0.75rem → 12px). */
export const SHELL_PANEL_COLUMN_STACK_GAP_PX = 12;
