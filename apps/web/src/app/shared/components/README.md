# components (shared)

Reusable **atomic UI** pieces (currently `chip`). Prefer importing from here before duplicating in features.

**Canonical spec:** [`docs/specs/component/filters/chip.md`](../../../../../../docs/specs/component/filters/chip.md)
