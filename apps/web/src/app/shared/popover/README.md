# Popover (`app-popover`)

**Normative spec:** `docs/specs/component/ui-primitives/popover.md`

Presentational shell only: `background`, `border`, `radius`, `shadow`, `z-index`. Parent owns `position` / coordinates, dismiss, and focus.
