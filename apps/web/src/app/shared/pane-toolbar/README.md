# pane-toolbar (shared)

**Toolbar pane** — three-slot **layout primitive** (`left` / `center` / `right`) for any route that needs the same chrome row (projects, media, workspace). Domain controls project into slots; overflow menu behavior is spec’d as deferred in the canonical doc.

**Canonical spec:** [`docs/specs/component/workspace/pane-toolbar.md`](../../../../../../docs/specs/component/workspace/pane-toolbar.md)
