/** Square control density for inline confirm actions. */
export type InlineConfirmSize = 'md' | 'sm';

/** Idle emphasis before arming — destructive vs primary remove. */
export type InlineConfirmTone = 'danger' | 'remove';

/** Parent row hover reveals the control until armed. */
export type InlineConfirmRevealParent = 'detail-row' | 'sidebar-row-wrap';

/** Detail-row grid slot when embedded in `.detail-row` five-column layout. */
export type InlineConfirmDetailRowSlot = 'r1' | 'r2';
