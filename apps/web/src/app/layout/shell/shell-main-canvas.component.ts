import { Component } from '@angular/core';

/** Generic content track. The map is one tenant, not the shell. */
@Component({
  selector: 'app-shell-main-canvas',
  standalone: true,
  templateUrl: './shell-main-canvas.component.html',
  styleUrl: './shell-main-canvas.component.scss',
})
export class ShellMainCanvasComponent {}
