# DEAD CODE — not built, not referenced

**DEAD CODE** — this directory is **not** part of the production Angular compilation (`tsconfig.app.json` excludes `src/app/archive/**/*.ts`). **Do not** import from here, **do not** cite this tree in specs or contracts, and **do not** copy patterns (SCSS, tokens, templates) from here. Kept for historical reference only. See root **`AGENTS.md`** → **Dead code (`apps/web/src/app/archive/`)**.
